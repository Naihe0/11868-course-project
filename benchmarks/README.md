# Benchmark Results

Results from `project/run_benchmark.py` will be stored here.

## Directory Structure

After running benchmarks:
```
benchmarks/
├── README.md              (this file)
└── results/
    └── benchmark_results.csv
```
